const uiOverlayImg = new Image();
const uiOverlayWidth = 83200;
const uiOverlayHeight = 600;
let gameFrame118 = 0;
const staggerFrames118 = 400;
const spriteAnimations118 = [];
let uiOverlayState = "move";
uiOverlayImg.src =
  "SpaceChaserSprites/BrokenSwitch/brokenSwitchAnimation.png";

const animationStates118 = [
  {
    name: "move",
    frames: 3,
  },
  {
    name: "noMove",
    frames: 1,
  },
];

animationStates118.forEach((state, index) => {
  let frames = {
    loc: [],
  };
  for (let i = 0; i < state.frames; i++) {
    let positionX = i * uiOverlayWidth;
    let positionY = index * uiOverlayHeight;
    frames.loc.push({ x: positionX, y: positionY });
  }
  spriteAnimations118[state.name] = frames;
});

function animation118() {
  let position =
    Math.floor(gameFrame118 / staggerFrames118) %
    spriteAnimations118[uiOverlayState].loc.length;
  let frameX = spriteAnimations118[uiOverlayState].loc[position].x;
  let frameY = spriteAnimations118[uiOverlayState].loc[position].y;
  ctx.drawImage(
    uiOverlayImg,
    frameX,
    frameY,
    uiOverlayWidth,
    uiOverlayHeight,
    0,
    0,
    uiOverlayWidth,
    uiOverlayHeight
  );
  gameFrame118++;
  requestAnimationFuiOverlay